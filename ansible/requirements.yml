---
# ==========================================
# Ansible Role Requirements
# ==========================================
# Install with: ansible-galaxy install -r requirements.yml

collections:
  - name: community.general
    version: ">=5.0.0"
  - name: ansible.posix
    version: ">=1.3.0"

roles:
  - name: xanmanning.k3s
    src: https://github.com/xanmanning/ansible-role-k3s.git
    version: main
    # Alternative: version: v3.4.0  # Specific version
