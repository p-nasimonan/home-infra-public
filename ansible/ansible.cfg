[defaults]
# インベントリファイル設定
inventory = ./inventory.yml

# ロール検索パス
roles_path = ./roles:/usr/share/ansible/roles

# ホストキー検認設定
host_key_checking = False

# 昇格設定
become_method = sudo
become_user = root
become_ask_pass = False

# 出力フォーマッティング設定
force_color = True
stdout_callback = yaml
display_skipped_hosts = False
display_ok_hosts = True

# ログ設定
#log_path = ./ansible.log

# 詳細度設定
#verbosity = 2

# 接続設定
timeout = 30
connection_timeout = 10

# Galaxy 設定
galaxy_server_list = galaxy

# 🚀 爆速設定 Part 1: 並列実行とファクトキャッシング
# 同時実行数（デフォルトは5）。ノード数に合わせて増やすと速い
forks = 20

[ssh_connection]
# 🚀 爆速設定 Part 2: SSH 最適化
# SSHパイプライニング（超重要）
# ファイル転送と実行を1回の接続でやる設定。劇的に速くなる。
pipelining = True

# SSHの接続を維持する（ControlPersist）
# 1回繋いだら60秒間は切断せず、次のタスクで使い回す
# ホストキーチェック無しで高速化
ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o PreferredAuthentications=publickey -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null

[galaxy]
server = https://galaxy.ansible.com